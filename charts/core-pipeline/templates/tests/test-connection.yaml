apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "core-pipeline.fullname" . }}-test-connection"
  labels:
    {{- include "core-pipeline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "core-pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
